package main

import (
	"fmt"
	"strings"
)
func main() {

	// Initial string
	 s := "Hello, World!"
	 fmt.Println("", s)

	 // converting to lower case
	 lowerS := strings.ToLower(s)
	 fmt.Println("", lowerS)
}
