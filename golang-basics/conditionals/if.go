package main

import "fmt"

func main() {
	// the if statement is used to check if a condition is met and execute the 
	// the code inside the if block only if the condition is met.
	isRaining := true

	if isRaining {
		fmt.Println("It's raining , take an umbrella")
	}
}
